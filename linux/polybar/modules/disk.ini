[module/disk]
type=custom/script
interval=60
exec=echo " $(df -h | grep "/dev/sda2" | awk '{print $5}')"

format=%{T2}<label>%{T-}
label-foreground = ${colors.fg}
label-background = ${colors.bg}
label-padding = 1
